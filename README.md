### TokenCreator contract.

# Create the tokens via ERC20Factory Contract.

#### Deploy

```bash
npx hardhat deploy
```

To deploy the contracts locally, add network in hardhat.config.ts and run

```bash
npx hardhat deploy network
```